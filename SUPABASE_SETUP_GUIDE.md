# Supabase 数据库设置指南

## 📝 操作步骤（5分钟完成）

### 步骤1：打开SQL编辑器
1. 登录 Supabase：https://supabase.com/dashboard
2. 选择你的项目
3. 点击左侧菜单的 **SQL Editor**
4. 点击 **New Query** 按钮

### 步骤2：复制SQL脚本
打开项目中的 `supabase_setup_simple.sql` 文件，复制全部内容

### 步骤3：粘贴并运行
1. 将SQL脚本粘贴到SQL编辑器中
2. 点击右下角的 **Run** 按钮
3. 等待执行完成（约5秒）
4. 看到 "Database setup completed!" 表示成功

### 步骤4：创建照片存储桶
1. 点击左侧菜单的 **Storage**
2. 点击 **New bucket**
3. 填写信息：
   - Name: `photos`
   - Public bucket: ✅ 勾选（设置为公开）
4. 点击 **Create bucket**

### 步骤5：验证表是否创建成功
在SQL编辑器中运行：
```sql
SELECT table_name
FROM information_schema.tables
WHERE table_schema = 'public'
ORDER BY table_name;
```

应该看到三个表：
- ✅ checklists
- ✅ checkins
- ✅ subscriptions

---

## 🚀 设置完成后

返回这里告诉我 "数据库已设置完成"，我们继续下一步测试！
